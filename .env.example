# Lectio — environment variable reference
# Copy to .env.local and fill in values as needed

# (No server-side variables required for the current build)
# Add consumer API keys here as integrations are built out
